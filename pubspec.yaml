name: myfootball
description: FootballTeam develop by TuyenNguyen.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2
  intl: ^0.15.7
  rxdart: ^0.19.0
  flutter_statusbarcolor: ^0.1.3
  http: ^0.12.0
  location: ^2.1.0
  timeago: ^2.0.10
  flutter_swiper: ^1.1.4
  share: ^0.5.3
  shared_preferences: ^0.4.3
  dio: ^2.1.2

dev_dependencies:
  flutter_launcher_icons: ^0.7.0
  flutter_test:
    sdk: flutter

flutter_icons:
  android: true
  ios: true
  image_path: 'assets/images/icn_logo.jpg'

# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # To add assets to your application, add an assets section, like this:
  assets:
  - assets/images/

  fonts:
  - family: regular
    fonts:
      - asset: assets/fonts/sans-regular.ttf
  - family: italic
    fonts:
      - asset: assets/fonts/sans_italic.ttf
  - family: semi-bold
    fonts:
      - asset: assets/fonts/sans_semibold.ttf
  - family: semi-bold-italic
    fonts:
      - asset: assets/fonts/sans_semibold_italic.ttf
  - family: bold
    fonts:
      - asset: assets/fonts/sans_bold.ttf
  - family: bold-italic
    fonts:
      - asset: assets/fonts/sans_bold_italic.ttf
